// Generated by forge proof from verify-cli.md
import { describe, test, expect } from 'bun:test';
import { promises as fs } from 'fs';
import path from 'path';

// ── CLI command registration ────────────────────────────────

describe('verify CLI: command registration in index.ts', () => {
  test('verify command is registered after proof in index.ts', async () => {
    const indexPath = path.join(import.meta.dirname, 'index.ts');
    const content = await fs.readFile(indexPath, 'utf-8');

    // verify command is registered
    expect(content).toContain(".command('verify')");

    // proof command appears before verify
    const proofIdx = content.indexOf(".command('proof')");
    const verifyIdx = content.indexOf(".command('verify')");
    expect(proofIdx).toBeGreaterThan(-1);
    expect(verifyIdx).toBeGreaterThan(-1);
    expect(proofIdx).toBeLessThan(verifyIdx);
  });

  test('verify command accepts proof-dir as positional argument', async () => {
    const indexPath = path.join(import.meta.dirname, 'index.ts');
    const content = await fs.readFile(indexPath, 'utf-8');

    // Check the argument definition near the verify command
    const verifyIdx = content.indexOf(".command('verify')");
    const sectionAfterVerify = content.slice(verifyIdx, verifyIdx + 500);
    expect(sectionAfterVerify).toContain('proof-dir');
  });

  test('verify command has --dry-run option', async () => {
    const indexPath = path.join(import.meta.dirname, 'index.ts');
    const content = await fs.readFile(indexPath, 'utf-8');

    const verifyIdx = content.indexOf(".command('verify')");
    const sectionAfterVerify = content.slice(verifyIdx, verifyIdx + 800);
    expect(sectionAfterVerify).toContain('--dry-run');
  });

  test('verify command has --cwd option', async () => {
    const indexPath = path.join(import.meta.dirname, 'index.ts');
    const content = await fs.readFile(indexPath, 'utf-8');

    const verifyIdx = content.indexOf(".command('verify')");
    const sectionAfterVerify = content.slice(verifyIdx, verifyIdx + 800);
    expect(sectionAfterVerify).toContain('--cwd');
  });

  test('verify command has --model option', async () => {
    const indexPath = path.join(import.meta.dirname, 'index.ts');
    const content = await fs.readFile(indexPath, 'utf-8');

    const verifyIdx = content.indexOf(".command('verify')");
    const sectionAfterVerify = content.slice(verifyIdx, verifyIdx + 800);
    expect(sectionAfterVerify).toContain('--model');
  });

  test('verify command has --quiet option', async () => {
    const indexPath = path.join(import.meta.dirname, 'index.ts');
    const content = await fs.readFile(indexPath, 'utf-8');

    const verifyIdx = content.indexOf(".command('verify')");
    const sectionAfterVerify = content.slice(verifyIdx, verifyIdx + 800);
    expect(sectionAfterVerify).toContain('--quiet');
  });

  test('verify command has --output-dir option', async () => {
    const indexPath = path.join(import.meta.dirname, 'index.ts');
    const content = await fs.readFile(indexPath, 'utf-8');

    const verifyIdx = content.indexOf(".command('verify')");
    const sectionAfterVerify = content.slice(verifyIdx, verifyIdx + 800);
    expect(sectionAfterVerify).toContain('--output-dir');
  });

  test('verify command has a description', async () => {
    const indexPath = path.join(import.meta.dirname, 'index.ts');
    const content = await fs.readFile(indexPath, 'utf-8');

    const verifyIdx = content.indexOf(".command('verify')");
    const sectionAfterVerify = content.slice(verifyIdx, verifyIdx + 200);
    expect(sectionAfterVerify).toContain('.description(');
  });
});

// ── Import structure ────────────────────────────────────────

describe('verify CLI: import structure', () => {
  test('index.ts imports runVerify from proof-runner', async () => {
    const indexPath = path.join(import.meta.dirname, 'index.ts');
    const content = await fs.readFile(indexPath, 'utf-8');

    expect(content).toContain("import { runVerify }");
    expect(content).toContain("./proof-runner.js");
  });

  test('runVerify is callable with VerifyOptions', async () => {
    const { runVerify } = await import('./proof-runner.js');
    expect(typeof runVerify).toBe('function');
  });
});

// ── Nested session guard ────────────────────────────────────

describe('verify CLI: nested session guard', () => {
  test('verify action calls guardNestedSession (uses SDK)', async () => {
    const indexPath = path.join(import.meta.dirname, 'index.ts');
    const content = await fs.readFile(indexPath, 'utf-8');

    // Find the verify action block
    const verifyIdx = content.indexOf(".command('verify')");
    const nextCommandIdx = content.indexOf(".command(", verifyIdx + 20);
    const verifySection = content.slice(verifyIdx, nextCommandIdx > 0 ? nextCommandIdx : verifyIdx + 1500);

    expect(verifySection).toContain('guardNestedSession()');
  });
});
